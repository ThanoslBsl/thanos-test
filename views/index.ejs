<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <table>
    <% for(var i=0; i < data.length; i++) { %>
       <tr>
          <td><%= data[i].steamid %></td>
          <td><%= data[i].mapname %></td>
          <td><%= data[i].name %></td>
          <td><%= data[i].runtimepro %></td>
          <td><%= i+1 %></td>
       </tr>
    <% } %>
    </table>
  </body>
</html>
